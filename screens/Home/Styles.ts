import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    center: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center'
    },
    header: {
        fontSize: 35,
        marginBottom: 15
    },
    goBackButtonText: {
        fontSize: 25,
        marginTop: 15,
        color: 'gray'
    }
});